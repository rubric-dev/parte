import { Canvas, Meta, Controls, ArgTypes } from "@storybook/blocks";
import * as IconButtonStories from "./IconButton.stories";

<Meta of={IconButtonStories} />

# IconButton

IconButton은 아이콘으로만 이루어진 Button 입니다.

<Canvas of={IconButtonStories.FillPrimary} />
<Controls of={IconButtonStories.FillPrimary} />

{/* <ArgTypes of={IconButtonStories.IconArgs} /> */}
