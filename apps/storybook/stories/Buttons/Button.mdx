import { Canvas, Meta, Controls, ArgTypes } from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

Button은 유저가 액션이나 이벤트를 호출할 수 있게 해주는 컴포넌트입니다.

<Canvas of={ButtonStories.FillPrimary} />
<Controls of={ButtonStories.FillPrimary} />
<ArgTypes of={ButtonStories.IconArgs} />
