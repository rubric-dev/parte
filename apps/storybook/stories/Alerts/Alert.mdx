import { Canvas, Meta, Story } from "@storybook/blocks";
import * as AlertStories from "./Alert.stories";

<Meta of={AlertStories} />

# Alert

Alert 문서입니다
Alert는 토스트에서도 사용되지만 인라인으로도 사용가능합니다.

<Canvas of={AlertStories.Default} />
